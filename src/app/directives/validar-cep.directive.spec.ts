import { ValidarCepDirective } from './validar-cep.directive';

describe('ValidarCepDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidarCepDirective();
    expect(directive).toBeTruthy();
  });
});
